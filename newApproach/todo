total_unsafPerformIO weiter untersuchen


>>= should not evaluate value

see if validation is better than notification

remove MVar from waitQ 
   performance vs memory
 should this be configurable?
 none of my testCases (besides memTest) produces memory leaks
